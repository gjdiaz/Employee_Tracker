
-- use JOIN to add department.id to updaterole.department_id

/* example
SELECT
  favorite_books.book_name AS name, book_prices.price AS price
FROM favorite_books
JOIN book_prices ON favorite_books.book_price = book_prices.id;
*/

SELECT * FROM department
FROM department.id
JOIN department ON course_names.department = department.id;

-- make queries for updating role
-- use JOIN to add updaterole.id to employee.role_id

SELECT * FROM --table1
FROM --table2
GROUP BY --table1;

-- make queries for updating employee info
SELECT * FROM employee